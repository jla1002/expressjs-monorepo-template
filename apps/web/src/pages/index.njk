{% extends "layouts/default.njk" %}

{% block page_content %}
  <h1 class="govuk-heading-xl">{{ title }}</h1>
  <p class="govuk-body-l">{{ welcome }}</p>
  {% if description %}
    <p class="govuk-body">{{ description }}</p>
  {% endif %}
{% endblock %}
